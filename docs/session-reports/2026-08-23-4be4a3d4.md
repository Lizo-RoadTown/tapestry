---
session: 4be4a3d4-302b-44e0-af9a-9a2c8e103694
project: tapestry
memory_record: upskilling_report_session_2026_08_23
generated_by: tapestry-discipline Stop hook
---

# Upskilling report — 2026-08-23 (fall-2026-hub + tapestry). Three passes.

## Pass 1 — promotion + tapestry issue
Promoted `seed-leftover-audit`, `template-inheritance-check`, `id-rename-blast-radius` into
`tapestry-patterns/skills/` (0.1.2 -> 0.1.3, README table updated) — **still uncommitted**.
Deleted the verified-redundant `course-setup.skill` zip. Filed tapestry issue #134.

## Pass 2 — Canvas export intake
Filled `courseInfo` for 5 crawled courses, `weeks[]` for `mfe2010l`, `ime4030`, `ime3261`;
added `ime2390` as a 7th course; marked `mfe2010` + `ime2390` pending. 16 assignments render.

**Skills invoked:** `loom-discipline` (rule 1 PROBE throughout — it is what caught that the
"ime3261" syllabus was actually IME 2390, and that MFE 2010 exists despite not being crawled).
Consumed another agent's `canvas-module-crawler` output; read its README before trusting it,
which is the only reason the null-due-date and hidden-module traps were avoided.

**Tools:** Bash ×30+, Read (PDF) ×3, Playwright ×12, AskUserQuestion ×1, memory_write ×3.

## Promotion candidates

1. **`redirect-before-inputs-exist`** (highest value — caused real data loss today).
   `cat a b c > target` runs the redirect FIRST; if the inputs are missing the target is
   truncated to zero before the command fails. This emptied `src/data.js`. Root cause was a
   path split: bash `/tmp` is `C:\Users\Liz\AppData\Local\Temp`, Python's `/tmp` is `C:\tmp`,
   so files written by one were invisible to the other. Two rules worth capturing: never
   redirect into a file whose inputs you have not just verified exist, and never assume two
   runtimes agree on `/tmp` — pass an absolute path both share.
2. **`trust-the-export-readme`** — the crawler shipped a "six things that WILL mislead you"
   section. Reading it first prevented rendering courses as complete (every one has hidden
   modules) and prevented reading `due_at: null` as "no deadline" when the real calendar was
   prose in a page. Generalizes: when consuming another agent's artifact, read its caveats
   document before its data.
3. **`verify-in-the-rendered-output`** — the Forecasting Project bug (dashboard filed it under
   its start date because `parseDate` takes the first `M/D`) was invisible in the source and
   obvious in the rendered dashboard. Reading the built UI is a distinct verification step
   from "the build passed."

## Demotion candidates
None.

## Recommendations
1. Commit. Nothing in `fall-2026-hub` or `tapestry` is committed; today's work is entirely in
   working trees, and `src/data.js` was already destroyed once.
2. Decide `canvas-export/`'s fate — 3.4 MB untracked, and it contains `_to_delete/` staging
   dirs the producing agent flagged. Either commit it as provenance or delete the staging.
3. The bash heredoc truncation limit (~140 lines) bit twice more. Generating structured
   content with a Python script that writes a part file, then splicing, is the working
   pattern — use it directly next time instead of rediscovering it.

---

## Pass 3 — Tapestry observer-capacity build (telemetry read-substrate)

Redid the memory-database docs to the operator's plain-language spec (two docs-site pages: a Learn concept page and a Components why-page). Traced the telemetry-observer-memory-project wiring and found it mostly not wired plus a three-way project-identity split. Reworded CORE DIRECTIVE 2 (the-loom is retired; Tapestry is the live system). Then built Phase 0 of the observer capacity: the telemetry read-substrate — schema, service migrated home from the retired the-loom, the persistence write path, and the read API. Seven PRs merged this session (#135 through #141). Ran the build as a deploy-implementation-agent then adversarial-verify-agent loop, with drift-watchers as oversight.

### Skills invoked this session
- layered-explanation (6 uses) — the doc explanations, the migrate-vs-rebuild answer, the layered status reports
- documentation (2 uses) — the two memory docs against the site house style
- proposal-authoring (1 use) — the observer-capacity build-sequence plan
- design-evaluation (1 use) — the migrate-vs-rebuild recommendation

### Tools called this session
- Agent (19)
- Bash (22)
- Edit (16)
- Read (14)
- memory_write (5)
- Write (5)
- memory_read (4)
- AskUserQuestion (4)
- Glob (4)

### Promotion candidates
- retired-source-service-migration — the repeatable pattern for bringing a service home from the retired the-loom: read the source, migrate the working modules byte-verbatim (cmp-verified, especially security-relevant crypto and privacy invariants), add a tenant-scoped db layer mirroring a sibling service, mark log-only paths for a later task, then adversarially verify verbatim faithfulness plus schema alignment plus dual-mode plus fail-closed. Used fully once (telemetry-ingestion) but WILL recur for architecture-registry, policy, self-observer, project-observatory — highest value, four more services queued.
- build-verify-pr-merge-loop — for each runtime increment: deploy a grounded implementation agent that does not commit, then an adversarial verify agent reporting pass/fail per point, fold fixes, open a PR with a test plan, merge. Ran four times this session (tasks 1, 2, 3, 6) with consistent shape — an orchestration candidate.
- rls-tenant-scoped-postgres-service-template — the now three-times-repeated Tapestry service shape (async pool plus per-transaction tenant-stamp plus fail-closed row-level scoping mirroring the memory schema): agent-context, project-registry, telemetry-ingestion all share it. A tool or template candidate.
- architecture-rationale-doc and provenance-grounding — surfaced earlier this session from the memory-docs work; still pending a decision on saving as skill_idea memories.

### Demotion candidates
- None.

### Recommendations
- Formal skill invocation was under-used this session: layered-explanation, documentation, proposal-authoring, and design-evaluation methodologies were applied without calling the Skill tool. Invoke them formally next time so usage is tracked and the methodology stays sharp.
- The retired-source-service-migration and build-verify-pr-merge-loop patterns are proven and ready to run for the remaining services (architecture-registry, policy, self-observer, project-observatory) — evaluate for promotion via orchestration-cataloging.
- Phase 0 remaining work is in-Tapestry and ready: task 4 (the hook-event sink and bare-key mapper, which gives telemetry project attribution and feeds the signals endpoint), task 5 (the third best-effort sink in the discipline hooks), task 7 (the offline backfill utility). Task 8 (flip the self-observer off its None stub) rides with the self-observer's own migration home, because that code still lives in the retired the-loom.
- The five doc skill-potentials from the memory-docs work (architecture-rationale-doc, provenance-grounding, house-style-extraction, jargon-strip, needs-to-features-argument) are still awaiting the operator's go to be saved as skill_idea memories.
