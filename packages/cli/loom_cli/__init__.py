"""loom-cli — command-line interface for the-loom.

Stdlib-only. Cross-platform. Subcommands:
  loom init    — onboard the current directory as a loom consuming project
  loom version — print version + diagnostic info
"""
__version__ = "0.1.0"
