fastapi==0.115.4
uvicorn[standard]==0.32.0

# Postgres + connection pool (Phase 1 candidate registry)
psycopg[binary]==3.2.3
psycopg-pool==3.2.4

# JWT signature verification (hosted-multitenant mode via auth_bridge)
python-jose[cryptography]==3.3.0

# Outbound HTTP for the skill-making bridge dispatcher
# (services/architecture-registry/promote_dispatcher.py)
httpx==0.27.2
