"""docs_mcp — stdio MCP server exposing the Tapestry documentation as tools.

Three tools: tapestry_docs_search, tapestry_docs_read, tapestry_docs_list.
Runs locally per-client via `python -m docs_mcp` (or `tapestry-docs-mcp` if
the console-script entry point is installed). No network, no hosting.

See README.md in the parent directory for install + .mcp.json wiring.
"""
__version__ = "0.1.0"
