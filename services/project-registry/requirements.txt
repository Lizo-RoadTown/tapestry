fastapi==0.115.4
uvicorn[standard]==0.32.0

# Postgres + connection pool (Phase 3 — Project Registry tables)
psycopg[binary]==3.2.3
psycopg-pool==3.2.4

# JWT signature verification (Phase 3 auth_bridge, hosted-multitenant mode)
python-jose[cryptography]==3.3.0
