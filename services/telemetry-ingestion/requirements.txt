fastapi==0.115.4
uvicorn[standard]==0.32.0

# Wire-contract models (bridge_models: pydantic v2, extra='forbid').
# FastAPI 0.115.x pulls a compatible pydantic 2.x; pinned explicitly so the
# privacy invariant's ConfigDict/Field behavior is reproducible.
pydantic==2.9.2

# Postgres + connection pool (Phase 0 task 2 — telemetry substrate, db.py).
# Versions matched to services/project-registry + services/agent-context so
# the shared RLS/pool pattern stays in lockstep across services.
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
